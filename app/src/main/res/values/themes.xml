<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base app theme — Compose takes over from here, so we just need no action bar -->
    <style name="Theme.TimelapsApp" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
